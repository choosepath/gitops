apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ingress.yaml
  - crt.yaml
  - clusterIssuer.yaml
  - traefik-middleware.yaml

components:
  - common
  - ui
  - auth-service
  - db
