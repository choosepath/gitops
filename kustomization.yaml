apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: choosepath-develop

resources:
  - ingress.yaml
  - crt.yaml
  - clusterIssuer.yaml

components:
  - common
  - ui
  - auth-service
  - db
